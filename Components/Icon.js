// Icons.js
import React from 'react';

export const HamburgetMenuClose = () => (
  <svg> SVG for close icon </svg>
);

export const HamburgetMenuOpen = () => (
  <svg> SVG for open icon </svg>
);
